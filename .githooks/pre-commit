#!/usr/bin/env sh
set -eu

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is required for pre-commit. Install: https://github.com/gitleaks/gitleaks"
  exit 1
fi

gitleaks protect --staged --redact --verbose
